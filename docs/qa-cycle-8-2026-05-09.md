# Cycle 8 QA Report - 2026-05-09

Branch: `agent/qa-cycle-8`

## Result

Cycle-8 verification is clean for the integrated unauthenticated/public surface. Lint, typecheck, and production build pass. The touched desktop and mobile routes render without visible error screens, console errors, or horizontal overflow. Touched public APIs return successful cached responses, and protected/body-parsing routes return controlled 4xx responses rather than 500s.

No release blockers were found.

## Routes Checked

Desktop `1440x900` and mobile `390x844`:

- `/discover`
- `/search?q=zzzz-cycle-8-no-results`
- `/profile-hub` redirected to `/login`
- `/u/4am`

## Interaction Checks

- `/discover`: category controls for Developers, Clients, and Studios responded without console errors or layout overflow.
- `/search?q=zzzz-cycle-8-no-results`: no-results state rendered with `0 results`; Users, Posts, and Hashtags tabs responded without console errors or layout overflow.
- `/u/4am`: unauthenticated Follow action stayed controlled and did not trigger a visible error screen or console error.

## API Checks

- `GET /api/search/users?q=a&limit=2` -> `200`, `Cache-Control: private, max-age=10, stale-while-revalidate=30`
- `GET /api/search/posts?q=a&limit=2` -> `200`, `Cache-Control: private, max-age=10, stale-while-revalidate=30`
- `GET /api/search/hashtags?q=dev&limit=2` -> `200`, `Cache-Control: private, max-age=10, stale-while-revalidate=30`
- `GET /api/discover?type=all` -> `200`, `Cache-Control: private, max-age=10, stale-while-revalidate=30`
- `GET /api/discover?type=people` -> `200`, `Cache-Control: private, max-age=10, stale-while-revalidate=30`
- `GET /api/polls` -> `400`
- `GET /api/polls?postId=cycle-8-qa-no-such-post` -> `404`
- `GET /api/reports` -> `401`
- `POST /api/reports` valid JSON -> `401`
- `POST /api/reports` invalid JSON -> `401`
- `GET /api/settings/messaging` -> `401`
- `POST /api/settings/messaging` invalid JSON -> `405`
- `GET /api/upload/signed-url` -> `405`
- `POST /api/upload/signed-url` valid JSON -> `401`
- `POST /api/upload/signed-url` invalid JSON -> `401`
- `POST /api/polls` invalid JSON -> `401`

## Commands Run

- `npm run lint` - passed
- `npx tsc --noEmit` - passed
- `npm run build` - passed
- PowerShell API smoke requests against `http://localhost:3028`
- Playwright route smoke script against `http://localhost:3028` for desktop and mobile
- Playwright interaction smoke script against `http://localhost:3028`

## Notes And Risks

- The in-app Browser backend was unavailable in this session (`agent.browsers.list()` returned no browsers), so route and interaction smoke coverage used the repo Playwright dependency.
- `/profile-hub` could only be verified as a signed-out redirect to `/login`; authenticated profile-hub polish was not exercised in this pass.
- Upload, reports, settings, and polls checks were kept to safe unauthenticated/auth-guard and malformed-body behavior. No real uploads, reports, polls, or settings mutations were submitted.
- The worktree already had untracked `tmp-next-start-cycle8.err.log` and `tmp-next-start-cycle8.out.log` files before QA; they were left untouched.

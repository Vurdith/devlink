# QA Cycle 5 Report - 2026-05-09

Branch: `agent/qa-cycle-5`

## Result

No release-blocking regressions found in the integrated cycle-5 surfaces during this pass.

## Findings

- No visible error screens found on the checked public, trust, search, messages, or notifications pages.
- No browser console errors or page errors found on the clean production smoke pass.
- No horizontal overflow found at `1366x900` desktop or `390x844` mobile on the checked routes.
- Public report form step flow behaved correctly: selecting an issue enables `Next`; the details step keeps `Submit Report` disabled until enough description text is entered; unauthenticated users see sign-in guidance.
- Navbar search submitted to `/search?q=reece` on both desktop and mobile.
- Unauthenticated API checks returned explicit auth errors for reports, verification requests, messages, and notifications.
- Poll utility validation tests passed; unauthenticated poll creation correctly returned `401`, and missing poll `postId` returned `400`.

## Routes Checked

Desktop and mobile:

- `/privacy`
- `/terms`
- `/report`
- `/verification`
- `/verify`
- `/search?q=reece`
- `/messages`
- `/notifications`

Interaction checks:

- `/report`: issue selection, disabled/enabled submit states, sign-in guidance.
- `/privacy`: navbar search input to `/search?q=reece`.

## APIs Checked

- `GET /api/reports` -> `401 {"error":"Sign in to view reports."}`
- `POST /api/reports` -> `401 {"error":"Sign in to submit a report."}`
- `GET /api/verification/requests` -> `401 {"error":"Sign in to view verification requests."}`
- `POST /api/verification/requests` -> `401 {"error":"Sign in to request verification."}`
- `GET /api/messages/conversations` -> `401 {"error":"Unauthorized"}`
- `GET /api/messages/threads` -> `401 {"error":"Unauthorized"}`
- `GET /api/messages/requests` -> `401 {"error":"Unauthorized"}`
- `GET /api/notifications` -> `401 {"error":"Unauthorized"}`
- `GET /api/notifications/unread-count` -> `401 {"error":"Unauthorized"}`
- `POST /api/notifications/mark-read` -> `401 {"error":"Unauthorized"}`
- `GET /api/polls` without `postId` -> `400 {"error":"Post ID required"}`
- `POST /api/polls` while unauthenticated -> `401 {"error":"Unauthorized"}`

## Commands Run

- `git status --short --branch`
- `git log --oneline -8`
- `git show --stat --oneline --decorate HEAD~5..HEAD`
- `npm run lint` - passed
- `npx tsc --noEmit` - passed
- `npm run build` - first attempt blocked by another active build; retry passed after that process finished
- `npx vitest run src/lib/__tests__/api-utils.test.ts src/lib/__tests__/validation-extended.test.ts` - passed, 18 tests
- Playwright smoke and interaction scripts against `http://127.0.0.1:3002`
- PowerShell `Invoke-WebRequest` API auth-state checks against `http://127.0.0.1:3002`

## Notes

- The in-app Browser backend was unavailable for this session (`iab` could not be acquired), so browser verification used local Playwright automation.
- An initial pass against an already-running `http://127.0.0.1:3001` server showed broken static chunk loads and mobile overflow. After `npm run build` passed and a fresh production server was started on port `3002`, those symptoms disappeared. Treat the 3001 result as stale-server noise, not a confirmed product regression.
- Authentication-backed happy paths were not exercised because no signed-in test session was available in this pass.
- Poll creation validation beyond unauthenticated behavior was covered by the targeted utility tests, not by an authenticated API create request.

/**
 * Simple solid background - no gradients for cleaner look
 */
export function AnimatedBackground() {
  return (
    <div className="fixed inset-0 -z-20 pointer-events-none bg-[#0a0a0f]" />
  );
}
